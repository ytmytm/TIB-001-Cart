MEMORY {
	ROM8000:	start = $8000, size=$2000, fill=yes, fillval=$ff, file=%O;
	STACK0101:	start = $0101, size=$00FF, define=yes;
}

FILES {
	%O: format = bin;
}

SEGMENTS {
	rom8000:	load = ROM8000, type = ro;
	romstack:	load = ROM8000, run = STACK0101, type = ro;
}

